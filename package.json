{
  "name": "oasis",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "engineStrict": true,
  "engines": {
    "node": "24"
  },
  "scripts": {
    "test": "npm run -w @navikt/oasis test",
    "lint": "biome check",
    "lint:ci": "biome ci",
    "build:app": "npm run build -w example-app",
    "build:lib": "npm run build -w @navikt/oasis",
    "type-check": "npm run type-check -w @navikt/oasis",
    "prepare": "husky"
  },
  "workspaces": [
    "libs/*",
    "example-app"
  ],
  "devDependencies": {
    "@biomejs/biome": "2.2.7",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.5"
  },
  "lint-staged": {
    "*.{js,ts,cjs,mjs,d.cts,d.mts,jsx,tsx,json,jsonc}": [
      "biome format --write --no-errors-on-unmatched",
      "biome lint --write --no-errors-on-unmatched"
    ]
  }
}
